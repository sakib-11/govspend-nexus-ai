{
  "name": "govspend-nexus-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Government Spend Audit & Procurement Intelligence System",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "poetry run uvicorn services.ingestion-svc.app:app --reload",
    "dev:frontend": "cd frontend/auditor-console && npm run dev",
    "build": "npm run build:services && npm run build:frontend",
    "build:services": "poetry build",
    "build:frontend": "cd frontend/auditor-console && npm run build",
    "test": "poetry run pytest",
    "test:coverage": "poetry run pytest --cov=services --cov-report=html",
    "lint": "poetry run ruff check . && npm run lint:frontend",
    "lint:frontend": "cd frontend/auditor-console && npm run lint",
    "format": "poetry run black . && npm run format:frontend",
    "format:frontend": "cd frontend/auditor-console && npm run format",
    "clean": "rm -rf **/__pycache__ **/.pytest_cache **/.coverage dist build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "prettier": "^3.0.0",
    "turbo": "^1.10.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
