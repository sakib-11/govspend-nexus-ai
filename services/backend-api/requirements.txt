# Production dependencies for the Backend API service.
# Install with:
#   pip install --no-cache-dir -r requirements.txt
#
# After installing, you can freeze exact hashes for deployment with:
#   pip freeze > requirements-lock.txt

# Web framework and server
fastapi>=0.110
uvicorn[standard]>=0.29

# Settings / validation
pydantic>=2.6
pydantic-settings>=2.2

# Env loading
python-dotenv>=1.0

# Database (to be used by the refactored data layer)
sqlalchemy>=2.0
psycopg2-binary>=2.9
asyncpg>=0.29
alembic>=1.13

# HTTP client (internal service calls, webhooks, etc.)
httpx>=0.27
